# Matcher 候选召回泛化审计

日期：2026-09-05

## 目标

本审计只回答未见优化轨迹的真实匹配是否在候选生成阶段被过滤，不评价
action-value 排序、Top-K 动作名次或 beam 是否最终选择该动作。真实标签为
Quartz `available_xfer_bindings_parallel` 枚举并按
`(source_id, binding_slots)` 合并后的全部合法 source/binding。

目标轨迹为：

- `barenco_tof_3/38_3`：16 个动作状态；
- `gf2^6_mult/370_2`：271 个动作状态，按模型训练语义切成不超过 64 步的因果窗口。

每条轨迹还另外生成 `max_actions=1` 的数据，用于丢掉动作历史的独立状态对照。

## 隔离检查

| 目标 | checkpoint 数据文件状态行（train+test+terminal） | holdout 状态行（含终态） | 重叠 graph hash | 重叠监督 graph hash | 共享动作标签 |
| --- | ---: | ---: | ---: | ---: | ---: |
| Barenco `38_3` | 25,840 | 17 | 2 | 0 | 0 |
| GF `370_2` | 31,020 | 272 | 0 | 0 | 0 |

Barenco 的两个重叠 hash 只来自其他轨迹的无标签 terminal graph；这些终态没有
matcher/action label，也不进入监督 loss。GF 是包括 terminal 在内的严格零状态重叠。
校准状态来自 checkpoint 合并数据中的固定测试部分，因此也包含在上述完整数据审计
范围内。

## 目标教师动作是否被过滤

| 轨迹/输入模式 | `r=0.95` | `r=0.99` | `r=0.99` 是否整链通过 |
| --- | ---: | ---: | --- |
| Barenco 因果历史 | 16/16 | 16/16 | 是 |
| Barenco 独立状态 | 13/16 | 16/16 | 是 |
| GF 64 步因果窗口 | 266/271 | 270/271 | 否，漏 1 步 |
| GF 独立状态 | 210/271 | 264/271 | 否，漏 7 步 |

因此，对 Barenco，未见 matcher 在 `r=0.99, max_source_matches=8192` 下满足
“目标轨迹动作一个不漏”，且不依赖教师动作历史。对 GF，历史显著改善召回，但标准
配置仍差一个动作，不能宣称完整通过。

## 全部 Quartz 真实匹配召回

| 轨迹/输入模式 | `r` | 保留/全部真实匹配 | 召回率 | 所有真实匹配均保留的状态 |
| --- | ---: | ---: | ---: | ---: |
| Barenco 因果历史 | 0.95 | 1,589/1,600 | 99.3125% | 14/16 |
| Barenco 因果历史 | 0.99 | 1,600/1,600 | 100.0000% | 16/16 |
| Barenco 独立状态 | 0.95 | 1,527/1,600 | 95.4375% | 10/16 |
| Barenco 独立状态 | 0.99 | 1,556/1,600 | 97.2500% | 10/16 |
| GF 64 步因果窗口 | 0.95 | 557,116/559,524 | 99.5696% | 3/271 |
| GF 64 步因果窗口 | 0.99 | 559,314/559,524 | 99.9625% | 228/271 |
| GF 独立状态 | 0.95 | 553,133/559,524 | 98.8578% | 0/271 |
| GF 独立状态 | 0.99 | 559,313/559,524 | 99.9623% | 193/271 |

总体召回 99.96% 仍不能保证 271 步轨迹零漏失；长轨迹只要有一个关键动作被删掉，
完整复现概率就是零。这也是必须同时报告 teacher-chain coverage 的原因。

## 候选过滤位置

Barenco 因果 `r=0.99`：

- eligible 粗 `(anchor, source)`：872,872；
- 阈值后：48,446，概率阶段过滤 94.45%；
- 全部真实匹配：1,600/1,600；
- 8192 cap 没有删除真实匹配，结构解码也没有额外删除真实匹配。

GF 因果 `r=0.99`：

- eligible 粗 `(anchor, source)`：157,843,833；
- 阈值后：3,136,749，概率阶段过滤 98.01%；
- source cap 后：2,216,710；
- 结构解码后：785,532；
- 559,524 个真实匹配中，107 个在概率阈值处丢失，103 个在 8192 cap 处丢失，
  结构解码没有额外删除已选中的真实匹配。

## GF 唯一因果教师漏点

标准 `r=0.99, max_source_matches=8192` 唯一遗漏的是零基 state 84，即人类计数的
第 85 步：

```text
source_id                 3752
binding                   (545, 515)
group                     near
raw logit                 -16.9071
r=0.99 near raw threshold -11.0090
all-eligible score rank   109,624 / 581,340
near-only score rank      13,417 / 57,582
```

它既低于概率阈值，也远超全局 8192 cap；只把 target recall 从 0.99 略微调高，
但继续使用全局 8192 cap，仍不能恢复该动作。

## Near reserve 救援探针

对 state 84 保留标准候选，并额外把 near 组中不受概率阈值限制的 Top16384 送入
结构解码：

```text
standard decoded candidates       2,880
near-reserve decoded candidates     287
deduplicated union                2,886
additional final candidates           6
teacher binding retained           true
```

因此 `r=0.99 + global cap 8192 + near pre-decode reserve 16384` 的并集可以把已知
GF 教师链从 270/271 补到 271/271。它没有改变动作排序，只改变 source 候选召回。
其主要成本发生在结构解码之前：该状态需要额外检查 16,384 个 near 粗候选，但结构
过滤后只增加 6 个最终 source/binding。

这个 rescue 参数是在查看 GF holdout 漏点后确定的，所以不能算原始配置的无偏泛化
成绩；它是下一分支设计候选生成方式时可直接采用和进一步验证的保守方案。要重新
获得严格泛化结论，应在新的、未用于选择 reserve 大小的整条轨迹上固定参数再测。

## 结论

1. Barenco `38_3` 的候选召回泛化验证通过：未见监督状态，`r=0.99` 下目标动作
   16/16，不依赖动作排序。
2. GF `370_2` 的标准配置接近但未完全通过：64 步因果输入为 270/271；99.9625%
   的全匹配召回不足以保证长轨迹零漏失。
3. 唯一 GF 因果漏点是 near 低分离群点。near Top16384 reserve 已实际恢复其完整
   binding，且结构解码后的去重候选只增加 6 个。
4. 如果下一分支的目标只是保证已知高质量轨迹动作仍有机会被搜索到，应保留
   `r=0.99` 标准集合，并增加独立的 near reserve；排序和 beam 生存可以在其后单独
   优化。
